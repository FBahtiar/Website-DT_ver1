import React from 'react';
import Classifier from '../components/Classifier';

const Deteksi = () => {
    return (
        <div>
            <Classifier />
        </div>
    );
};

export default Deteksi;
